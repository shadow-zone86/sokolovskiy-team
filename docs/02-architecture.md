# Глава 2: Архитектурные решения 🏗️

> Обзор архитектуры, паттерны проектирования и принципы SOLID

---

## 📐 Обзор архитектуры

Проект построен на **трёх столпах**:

1. **Feature-Sliced Design (FSD)** — методология организации кода
2. **SOLID принципы** — принципы объектно-ориентированного проектирования
3. **Provide/Inject + Pinia** — инверсия зависимостей (контент через store и inject)

Эта комбинация обеспечивает:

- ✅ Масштабируемость — легко добавлять новые секции и фичи
- ✅ Поддерживаемость — код легко понять и изменить
- ✅ Тестируемость — чистая логика в lib, покрыта Vitest
- ✅ Переиспользуемость — виджеты не зависят от конкретного источника данных

---

## 🎯 SOLID принципы

### S — Single Responsibility (Единственная ответственность)

**Принцип**: Каждый модуль имеет только одну причину для изменения.

#### ✅ Правильно: Разделение ответственностей

```typescript
// shared/config/siteContent.ts — только данные
export const siteContent = { profile: {...}, nav: {...} }

// features/section-nav/lib/getCurrentSectionIndex.ts — только расчёт индекса
export function getCurrentSectionIndex(ratios, sectionOrder): number { ... }

// widgets/header/ui/Header.vue — только отображение шапки
// Получает контент через inject(SITE_CONTENT_STORE)
```

#### ❌ Неправильно: Смешивание ответственностей

```typescript
// ❌ Компонент делает всё: и данные, и логику, и UI
// Виджет не должен импортировать конфиг напрямую и содержать бизнес-логику
```

---

### O — Open/Closed (Открыт для расширения, закрыт для модификации)

**Принцип**: Расширение через новые слайсы и данные, без изменения существующего кода.

- Добавление новой секции — новый виджет + правка только `PageHome.vue` и конфига
- Новый вариант кнопки — через props `variant` в `UiButton`
- Новый контент — правка `siteContent` или замена источника в store

---

### L — Liskov Substitution (Подстановка Лисков)

В проекте нет иерархий классов; компоненты используют композицию и слоты. `UiButton` рендерит то `<a>`, то `<button>` с единым интерфейсом (slot, пропсы) — подстановка сохраняет поведение.

---

### I — Interface Segregation (Разделение интерфейсов)

- Виджеты используют из store только **content** (и нужные поля)
- Тип **SiteContentStore** = `{ content: SiteContent }` — узкий контракт
- Пропсы компонентов узкие (Button: variant, type, href, disabled)

---

### D — Dependency Inversion (Инверсия зависимостей)

**Принцип**: Модули верхнего уровня не должны зависеть от модулей нижнего уровня; оба зависят от абстракций.

#### ✅ Правильно: Через provide/inject

```typescript
// app/App.vue — предоставляет реализацию
provide(SITE_CONTENT_STORE, useSiteContentStore())

// shared/config — ключ и тип (абстракция)
export const SITE_CONTENT_STORE: InjectionKey<SiteContentStore> = Symbol('siteContentStore')
export type SiteContentStore = { content: SiteContent }

// widgets/header/ui/Header.vue — зависит от абстракции
const { content } = inject(SITE_CONTENT_STORE)!
const { profile, contacts } = content
```

Виджеты и фичи **не импортируют** `siteContent` или app; они зависят только от ключа и типа в shared. Реализацию (Pinia store) предоставляет app.

#### ❌ Неправильно: Прямая зависимость

```typescript
// ❌ Виджет импортирует конфиг напрямую
import { siteContent } from '@/shared/config/siteContent'
const { profile } = siteContent
```

---

## 🏗️ Паттерны в проекте

### 1. Единственный источник правды (Single Source of Truth)

Контент сайта хранится в **shared/config/siteContent.ts**. App store инициализируется этими данными и предоставляет их через provide. Все потребители получают данные через inject — один канал данных.

### 2. Чистые функции в features/lib

В `features/section-nav/lib/` только чистые функции: `getCurrentSectionIndex(ratios, sectionOrder)`, `formatPageNum(index)`. Они не зависят от Vue, DOM или конфига — легко тестировать Vitest.

### 3. Публичный API слайсов

Каждый слайс экспортирует только необходимое через `index.ts`. Импорты идут из `@/widgets/header`, `@/shared/config`, а не из внутренних файлов.

---

## 📚 Дополнительные материалы

- [FSD Принципы](./03-fsd-principles.md)
- [Provide/Inject и Паттерны](./04-provide-inject-and-patterns.md)
- [Данные и конфиг](./05-data-and-config.md)
