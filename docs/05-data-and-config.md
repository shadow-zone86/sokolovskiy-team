# Глава 5: Данные и конфиг 📦

> Контент сайта, Pinia store, типы, расширение данных

---

## 📋 Содержание

1. [Источник данных](#источник-данных)
2. [Структура siteContent](#структура-sitecontent)
3. [Типы и публичный API](#типы-и-публичный-api)
4. [Расширение контента](#расширение-контента)
5. [Подключение API в будущем](#подключение-api-в-будущем)

---

## 📍 Источник данных

Контент сайта-визитки хранится в одном месте:

- **Файл:** `src/shared/config/siteContent.ts`
- **Экспорт:** через `src/shared/config/index.ts` (публичный API)

Данные используются так:

1. **app/store/siteContent.ts** импортирует `siteContent` из `@/shared/config` и инициализирует state Pinia.
2. **App.vue** вызывает `useSiteContentStore()` и передаёт store в дерево через `provide(SITE_CONTENT_STORE, ...)`.
3. Виджеты и фичи получают store через `inject(SITE_CONTENT_STORE)` и используют `content` (реактивно).

Прямой импорт `siteContent` в виджетах/фичах не используется — только через store и inject.

---

## 📐 Структура siteContent

Основные блоки (актуально на момент документации):

| Ключ | Описание |
|------|----------|
| `profile` | Имя, должность, теглайн, опыт, метрики (проекты, команды, наставничество) |
| `contacts` | Телефон, Telegram, CTA-кнопки |
| `footer` | Копирайт |
| `hero` | Заголовок, лид-тексты |
| `about` | Секция «О себе» (заголовок, параграфы) |
| `skillsTitle`, `skillsLead`, `skillsBlocks` | Секция «Навыки и подход» |
| `servicesTitle`, `services` | Секция «Направления работы» |
| `metricsTitle`, `metricsLead`, `metricsNote`, `metrics` | Секция «В цифрах» |
| `portfolioTitle`, `portfolioLead`, `portfolio` | Секция «Примеры работ» |
| `nav` | Тексты навигации, sectionTitles |
| `sections` | Id секций (hero, about, skills, …) |
| `sectionOrder` | Порядок секций для навигации |

Тип контента выводится из объекта: `export type SiteContent = typeof siteContent`.

---

## 🔧 Типы и публичный API

### shared/config/index.ts

- `appTitle` — строка (название приложения)
- `siteContent` — объект контента
- `SiteContent` — тип контента
- `SiteContentStore` — тип для inject: `{ content: SiteContent }`
- `SITE_CONTENT_STORE` — символ для provide/inject (`InjectionKey<SiteContentStore>`)

Импорт конфига в коде — только из `@/shared/config`, не из `@/shared/config/siteContent`.

### Использование в компонентах

```typescript
const { content } = inject(SITE_CONTENT_STORE)!
const { profile, nav, sectionOrder } = content
```

TypeScript знает тип `content` благодаря `InjectionKey<SiteContentStore>` и типу `SiteContentStore`.

---

## 📝 Расширение контента

### Добавление новой секции

1. Добавить данные в `siteContent` в `shared/config/siteContent.ts` (новый ключ или расширение существующего).
2. При необходимости добавить id в `sections` и элемент в `sectionOrder`, обновить `nav.sectionTitles`.
3. Создать виджет в `widgets/<new-section>/` (ui + index).
4. Подключить виджет в `pages/home/PageHome.vue`.

Виджеты по-прежнему берут данные только через inject; изменения в конфиге и store достаточны.

### Добавление нового поля в существующий блок

Добавить поле в объект в `siteContent.ts`. Тип обновится автоматически (`typeof siteContent`). В виджете использовать, например: `content.profile.newField`.

---

## 🔮 Подключение API в будущем

Чтобы перейти с статического конфига на данные с API:

1. **shared/config** — оставить типы и ключ `SITE_CONTENT_STORE`. Убрать или оставить статический `siteContent` как fallback.
2. **app/store/siteContent.ts** — добавить action, например `fetchContent()`, который вызывает API и делает `this.content = response`. При инициализации можно оставить текущий контент или сразу вызывать fetch.
3. Виджеты и фичи **не менять** — они по-прежнему используют `inject(SITE_CONTENT_STORE)!.content`.

Источник данных меняется только в app и при необходимости в shared (например, API-клиент в shared/api). Соблюдается Dependency Inversion.

---

## 📚 Дополнительные материалы

- [Архитектурные решения](./02-architecture.md)
- [Provide/Inject и Паттерны](./04-provide-inject-and-patterns.md)
- [FSD Принципы](./03-fsd-principles.md)
