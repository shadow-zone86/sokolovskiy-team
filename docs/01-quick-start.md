# Глава 1: Быстрый старт 🚀

> Всё, что нужно для запуска проекта за несколько минут

---

## 📋 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** — версия 18.x или выше
- **npm** — версия 9.x или выше
- **Git** — для клонирования репозитория

### Проверка версий

```bash
node --version   # v18.x.x или выше
npm --version   # 9.x.x или выше
git --version   # 2.x.x
```

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd sokolovskiy-team
```

### 2. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты из `package.json`:

- Vue 3, Vue Router, Pinia
- TypeScript
- Vite
- Vitest, Storybook
- ESLint, Sass

---

## 🚀 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Проект будет доступен по адресу: `http://localhost:5173` (порт может отличаться — смотрите вывод Vite).

### Production сборка

```bash
npm run build
npm run preview
```

`preview` запускает локальный сервер для просмотра собранного приложения.

---

## 📁 Структура проекта

```
sokolovskiy-team/
├── src/
│   ├── app/              # Инициализация, роутер, store, стили
│   ├── pages/            # Страницы (композиция виджетов)
│   ├── widgets/         # Виджеты
│   ├── features/        # Фичи (section-nav)
│   ├── entities/        # Сущности (типы)
│   └── shared/          # Конфиг, lib, стили, ui
├── docs/                 # Документация
├── public/               # Статические файлы
├── vite.config.ts        # Конфигурация Vite
├── tsconfig.json         # Конфигурация TypeScript
└── package.json
```

---

## 🎯 Следующие шаги

После успешного запуска:

1. Откройте `http://localhost:5173` в браузере
2. Изучите [Архитектурные решения](./02-architecture.md)
3. Прочитайте [FSD Принципы](./03-fsd-principles.md)
4. Изучите [Данные и конфиг](./05-data-and-config.md)

---

## ❓ Решение проблем

### Порт уже занят

Vite автоматически предложит следующий свободный порт (5174, 5175 и т.д.).

### Ошибки TypeScript

```bash
npm run typecheck
```

### Очистка кеша

```bash
rm -rf node_modules/.vite
npm run dev
```

---

**Далее**: [Глава 2: Архитектурные решения](./02-architecture.md) 🏗️
